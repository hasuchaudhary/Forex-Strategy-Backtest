name: Forex Strategy Backtest

on: [push, workflow_dispatch]

jobs:
  run-backtest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        # અહીં આપણે સીધું જ pip install કમાન્ડ આપી દીધો છે 
        run: pip install backtrader requests
        
      - name: Run Backtest
        run: python strategy.py
